<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TestXamarinFormsApp"
             x:Class="TestXamarinFormsApp.MainPage">
    <ListView ItemTapped="OnItemTapped"  ItemsSource="{Binding ListViewSource}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50" />
                        </Grid.ColumnDefinitions>
                        <Image Source="{Binding Image}" Grid.Row="0" Grid.Column="0" />
                        <Label Text="{Binding Title}" Font="Medium"  Grid.Row="0" Grid.Column="1" XAlign="Center" />
                        <Label Text="{Binding Description}" Font="Small"  Grid.Row="0" Grid.Column="2" XAlign="Center"/>
                    </Grid>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>