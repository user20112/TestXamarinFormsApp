<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TestXamarinFormsApp"
             x:Class="TestXamarinFormsApp.MainPage">
    <ListView ItemTapped="OnItemTapped"  ItemsSource="{Binding ListViewSource}"   >
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Padding="15, 5, 5, 5" HorizontalOptions="StartAndExpand" Orientation="Horizontal">
                        <Image Source="{Binding Image}" />
                        <Label Text="{Binding Title}" Font="10" />
                        <Label Text="{Binding Description}" Font="8" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>